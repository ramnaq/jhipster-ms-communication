apply plugin: "war"

bootWar {
    mainClassName = "com.leaf.client.ClientApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
